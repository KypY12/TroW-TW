// const fs = require("fs");
// const year = 2019;
// const yearFile = (year - 1) + '-' + year;
// const stations = require('./' + yearFile + '.json');

// let stationNames = [];
// stations.forEach(station => {
//     stationNames.push(station.stationName);
// });

// fs.writeFile("./" + yearFile + "names.json", JSON.stringify(stationNames, null, 4), (err) => { });